// main function of Jack, with a Golang flavor
func main() {
    var x array
    for j:=0; j<16; j++ {
        x = array.new(4095)
        x[0] = j
        print(x[0])
        print(x)
        // comment this out and we go out of memory!
        x.dispose()
    }
    return
}
