// main function of Jack, with a Golang flavor
func main() {
    math.init()
    screen.init()
    for r:=0; r<256; r++ {
        for c:=0; c<32; c++ {
            screen.drawWord(4095, r, c)
        }
    }
    return
}
