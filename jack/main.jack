// main function of Jack, with a Golang flavor
func main() {
    b := dict{}
    b := dict.newleaf("b", 42)
    key := "a"
    value := 96
    b.insert(key, value)
    key = "c"
    value = 4
    b.insert(key, value)
    key := "c"
    print(b.search(key))
    b.print()
    return
}
